"use client"

import { useState } from 'react'
import { motion } from 'framer-motion'
import { 
  User, 
  Settings, 
  BarChart3, 
  Key, 
  CreditCard, 
  Bell, 
  Shield, 
  LogOut,
  Menu,
  X,
  Home,
  MessageSquare,
  Activity,
  Zap
} from 'lucide-react'
import { useAuth } from '@/lib/auth'

export default function DashboardPage() {
  const [currentPage, setCurrentPage] = useState('overview')
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const { user, logout } = useAuth()

  const navigation = [
    { id: 'overview', name: 'Overview', icon: Home },
    { id: 'usage', name: 'Usage Analytics', icon: BarChart3 },
    { id: 'api-keys', name: 'API Keys', icon: Key },
    { id: 'playground', name: 'Playground', icon: MessageSquare },
    { id: 'billing', name: 'Billing', icon: CreditCard },
    { id: 'settings', name: 'Settings', icon: Settings },
  ]

  const renderPage = () => {
    switch (currentPage) {
      case 'overview':
        return <OverviewPage user={user} />
      case 'usage':
        return <UsageAnalyticsPage />
      case 'api-keys':
        return <APIKeysPage />
      case 'playground':
        return <PlaygroundPage />
      case 'billing':
        return <BillingPage />
      case 'settings':
        return <SettingsPage />
      default:
        return <OverviewPage user={user} />
    }
  }

  return (
    <div className="min-h-screen bg-black">
      {/* Mobile sidebar backdrop */}
      {sidebarOpen && (
        <div 
          className="fixed inset-0 z-40 bg-black/50 lg:hidden"
          onClick={() => setSidebarOpen(false)}
        />
      )}

      {/* Sidebar */}
      <div className={`fixed inset-y-0 left-0 z-50 w-64 bg-white/5 border-r border-white/10 transform transition-transform duration-300 ease-in-out lg:translate-x-0 lg:static lg:inset-0 ${
        sidebarOpen ? 'translate-x-0' : '-translate-x-full'
      }`}>
        <div className="flex h-full flex-col">
          {/* Logo */}
          <div className="flex h-16 shrink-0 items-center px-6 border-b border-white/10">
            <a href="/" className="flex items-center gap-2">
              <div className="h-8 w-8 rounded-full bg-gradient-to-r from-blue-500 to-purple-600 flex items-center justify-center">
                <span className="text-sm font-bold text-white">KΩ</span>
              </div>
              <span className="font-semibold text-white">Knight-Omega</span>
            </a>
          </div>

          {/* Navigation */}
          <nav className="flex-1 px-4 py-6 space-y-2">
            {navigation.map((item) => {
              const isActive = currentPage === item.id
              return (
                <button
                  key={item.id}
                  onClick={() => {
                    setCurrentPage(item.id)
                    setSidebarOpen(false)
                  }}
                  className={`w-full flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors ${
                    isActive
                      ? 'bg-gradient-to-r from-blue-500/20 to-purple-600/20 text-white border border-blue-500/30'
                      : 'text-white/60 hover:text-white hover:bg-white/5'
                  }`}
                >
                  <item.icon className="w-4 h-4" />
                  {item.name}
                </button>
              )
            })}
          </nav>

          {/* User profile */}
          <div className="px-4 py-6 border-t border-white/10">
            <div className="flex items-center gap-3 px-3 py-2">
              <div className="w-8 h-8 rounded-full bg-gradient-to-r from-blue-500 to-purple-600 flex items-center justify-center">
                <User className="w-4 h-4 text-white" />
              </div>
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium text-white truncate">
                  {user?.name || user?.email}
                </p>
                <p className="text-xs text-white/60 truncate">
                  {user?.email}
                </p>
              </div>
            </div>
            <button
              onClick={logout}
              className="w-full mt-2 flex items-center gap-3 px-3 py-2 text-sm font-medium text-white/60 hover:text-white hover:bg-white/5 rounded-lg transition-colors"
            >
              <LogOut className="w-4 h-4" />
              Sign out
            </button>
          </div>
        </div>
      </div>

      {/* Main content */}
      <div className="lg:pl-64">
        {/* Top bar */}
        <div className="sticky top-0 z-40 flex h-16 shrink-0 items-center gap-x-4 border-b border-white/10 bg-black/50 backdrop-blur-sm px-4 sm:gap-x-6 sm:px-6 lg:px-8">
          <button
            type="button"
            className="-m-2.5 p-2.5 text-white/60 hover:text-white lg:hidden"
            onClick={() => setSidebarOpen(true)}
          >
            <Menu className="h-6 w-6" />
          </button>

          <div className="flex flex-1 gap-x-4 self-stretch lg:gap-x-6">
            <div className="flex items-center gap-x-4 lg:gap-x-6">
              <h1 className="text-lg font-semibold text-white capitalize">
                {currentPage.replace('-', ' ')}
              </h1>
            </div>
            <div className="flex items-center gap-x-4 lg:gap-x-6 ml-auto">
              <div className="flex items-center gap-2 text-sm text-white/60">
                <div className="w-2 h-2 bg-green-400 rounded-full animate-pulse" />
                <span>API Status: Online</span>
              </div>
            </div>
          </div>
        </div>

        {/* Page content */}
        <main className="py-6">
          <div className="px-4 sm:px-6 lg:px-8">
            {renderPage()}
          </div>
        </main>
      </div>
    </div>
  )
}

// Dashboard Overview Page
function OverviewPage({ user }) {
  return (
    <div className="space-y-6">
      {/* Welcome Header */}
      <div className="bg-gradient-to-r from-blue-500/10 to-purple-600/10 border border-blue-500/20 rounded-lg p-6">
        <h2 className="text-2xl font-bold text-white mb-2">
          Welcome back, {user?.name || 'User'}! 👋
        </h2>
        <p className="text-white/60">
          Here's what's happening with your Knight-Omega account today.
        </p>
      </div>

      {/* Stats Grid */}
      <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
        {[
          { 
            name: 'API Requests', 
            value: '12,847', 
            change: '+12%', 
            changeType: 'positive',
            icon: Activity 
          },
          { 
            name: 'Active Models', 
            value: '8', 
            change: '+2', 
            changeType: 'positive',
            icon: Zap 
          },
          { 
            name: 'Usage This Month', 
            value: '89%', 
            change: '-5%', 
            changeType: 'negative',
            icon: BarChart3 
          },
          { 
            name: 'API Keys', 
            value: '3', 
            change: '0', 
            changeType: 'neutral',
            icon: Key 
          },
        ].map((stat) => (
          <div key={stat.name} className="bg-white/5 border border-white/10 rounded-lg p-6">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-white/60">{stat.name}</p>
                <p className="text-2xl font-bold text-white">{stat.value}</p>
              </div>
              <div className="w-10 h-10 rounded-lg bg-gradient-to-r from-blue-500/20 to-purple-600/20 flex items-center justify-center">
                <stat.icon className="w-5 h-5 text-blue-400" />
              </div>
            </div>
            <div className="mt-4">
              <span className={`text-sm font-medium ${
                stat.changeType === 'positive' ? 'text-green-400' :
                stat.changeType === 'negative' ? 'text-red-400' :
                'text-white/60'
              }`}>
                {stat.change}
              </span>
              <span className="text-sm text-white/60 ml-1">from last month</span>
            </div>
          </div>
        ))}
      </div>

      {/* Recent Activity */}
      <div className="bg-white/5 border border-white/10 rounded-lg">
        <div className="px-6 py-4 border-b border-white/10">
          <h3 className="text-lg font-semibold text-white">Recent Activity</h3>
        </div>
        <div className="p-6">
          <div className="space-y-4">
            {[
              { 
                action: 'Generated text using GPT-4o', 
                time: '2 minutes ago', 
                status: 'success' 
              },
              { 
                action: 'Created Knight Omega key "Production"', 
                time: '1 hour ago', 
                status: 'success' 
              },
              { 
                action: 'Upgraded to Pro plan', 
                time: '1 day ago', 
                status: 'success' 
              },
              { 
                action: 'Used Claude 3.5 Sonnet for analysis', 
                time: '2 days ago', 
                status: 'success' 
              },
            ].map((activity, index) => (
              <div key={index} className="flex items-center gap-4">
                <div className="w-2 h-2 bg-green-400 rounded-full" />
                <div className="flex-1">
                  <p className="text-sm text-white">{activity.action}</p>
                  <p className="text-xs text-white/60">{activity.time}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Quick Actions */}
      <div className="grid grid-cols-1 gap-6 lg:grid-cols-2">
        <div className="bg-white/5 border border-white/10 rounded-lg p-6">
          <h3 className="text-lg font-semibold text-white mb-4">Quick Actions</h3>
          <div className="space-y-3">
            {[
              { name: 'Create Knight Omega key', icon: Key },
              { name: 'View usage analytics', icon: BarChart3 },
              { name: 'Test models in playground', icon: MessageSquare },
              { name: 'Update billing information', icon: CreditCard },
            ].map((action) => (
              <button
                key={action.name}
                className="w-full flex items-center gap-3 px-3 py-2 text-sm text-white/60 hover:text-white hover:bg-white/5 rounded-lg transition-colors"
              >
                <action.icon className="w-4 h-4" />
                {action.name}
              </button>
            ))}
          </div>
        </div>

        <div className="bg-white/5 border border-white/10 rounded-lg p-6">
          <h3 className="text-lg font-semibold text-white mb-4">Model Performance</h3>
          <div className="space-y-4">
            {[
              { model: 'GPT-4o', requests: 3241, avgResponse: '1.2s' },
              { model: 'Claude 3.5', requests: 2891, avgResponse: '0.9s' },
              { model: 'Gemini Pro', requests: 1756, avgResponse: '1.1s' },
            ].map((model) => (
              <div key={model.model} className="flex items-center justify-between">
                <div>
                  <p className="text-sm font-medium text-white">{model.model}</p>
                  <p className="text-xs text-white/60">{model.requests} requests</p>
                </div>
                <div className="text-right">
                  <p className="text-sm text-white/60">Avg response</p>
                  <p className="text-sm font-medium text-white">{model.avgResponse}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}

// Usage Analytics Page
function UsageAnalyticsPage() {
  return (
    <div className="space-y-6">
      <div className="bg-white/5 border border-white/10 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-white mb-4">Usage Analytics</h2>
        <p className="text-white/60">Detailed analytics coming soon...</p>
      </div>
    </div>
  )
}

// API Keys Page
function APIKeysPage() {
  return (
    <div className="space-y-6">
      <div className="bg-white/5 border border-white/10 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-white mb-4">API Key Management</h2>
        <p className="text-white/60">API key management interface coming soon...</p>
      </div>
    </div>
  )
}

// Playground Page
function PlaygroundPage() {
  return (
    <div className="space-y-6">
      <div className="bg-white/5 border border-white/10 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-white mb-4">Interactive Playground</h2>
        <p className="text-white/60">API playground coming soon...</p>
      </div>
    </div>
  )
}

// Billing Page
function BillingPage() {
  return (
    <div className="space-y-6">
      <div className="bg-white/5 border border-white/10 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-white mb-4">Billing & Plans</h2>
        <p className="text-white/60">Billing management coming soon...</p>
      </div>
    </div>
  )
}

// Settings Page
function SettingsPage() {
  return (
    <div className="space-y-6">
      <div className="bg-white/5 border border-white/10 rounded-lg p-6">
        <h2 className="text-xl font-semibold text-white mb-4">Account Settings</h2>
        <p className="text-white/60">Settings panel coming soon...</p>
      </div>
    </div>
  )
}